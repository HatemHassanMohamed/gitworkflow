name: Incident Workflow

on:
  repository_dispatch:
    types:
      - incident_report

jobs:
  handle-incident:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "incident"
